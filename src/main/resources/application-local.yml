spring:
  jpa:
    database: MYSQL
    show-sql: false
    hibernate:
      ddl-auto: none
  datasource:
    platform: org.hibernate.dialect.MySQL5InnoDBDialect
    url: jdbc:mysql://localhost:3306/odonto-api
    username: root
    password: root_1234
    driverClassName: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null

amazon:
  dynamodb:
    endpoint: http://localhost:8000/
  aws:
    accesskey: TEST_KEY
    secretkey: PASSWORD123
    region: us-east-1
